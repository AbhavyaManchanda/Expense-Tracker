import React from "react";
const Title = ({ title }) => {
    return (
        <div className="text-2xl 
        2xl:text-3xl font-bold text-gray-800 mb-4">{title}</div>
    );
};
export default Title;